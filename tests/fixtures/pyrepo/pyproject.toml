[project]
name = "shop"
version = "0.1.0"
dependencies = ["fastapi>=0.115", "sqlalchemy>=2.0", "pydantic>=2.9"]

[project.optional-dependencies]
cache = ["redis>=5.0"]

[dependency-groups]
dev = ["pytest>=8.0"]
